"""Scoring layer — the three separated, confidence-ranged scores (DOK 1/2-3/4).

See :mod:`scoring.scores`. Pure stdlib, no GUI import, no SQL math on the live
path (retrievability comes from :mod:`gap.mastery`, which computes it in Python).
"""
